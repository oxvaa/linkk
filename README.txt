LINK 3.5 · Pulse — LINK Official DM Fix
========================================

LINK Official now uses the same visual hierarchy as a normal LINK direct-message chat.

Fixed:
- Header now uses the same centered avatar/name/status stack as normal DM chats.
- Removed duplicate verified badge from the header avatar.
- Replaced the oversized full-width system banner with the same compact pill style used by Encrypted chat.
- Official announcements render as normal incoming DM bubbles, not group-chat rows.
- Removed repeated sender avatar/name from each official message.
- Timestamps now use compact HH:MM formatting instead of full date + seconds.
- Disabled composer keeps the exact standard chat geometry.
- LINK Official hardcoded English strings now respect Czech app language.
- Launcher cache build bumped to 353.
